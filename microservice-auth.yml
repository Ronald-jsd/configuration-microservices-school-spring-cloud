
server:
  port: 0

spring:
  application:
    name: microservice-auth
  datasource:
    url: jdbc:mysql://localhost:3306/auth-db?createDatabaseIfNotExist=true
    username: root
    password: mysqlronald

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  instance:
    instance-id: ${spring.application.name}:${random.uuid}

  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    #fetch-registry: true
    #register-with-eureka: true
