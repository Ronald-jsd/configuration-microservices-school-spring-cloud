server:
  port: 8761

spring:
  application:
    name:
      mcsv-eureka

eureka:
  instance:
    hostname: eureka
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://microservice-eureka:8761/eureka
